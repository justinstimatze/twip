# Legacy CRA app imports some transitive deps directly (e.g. brace via react-ace).
# Hoist to a flat node_modules so those resolve, matching the npm/yarn layout it was built against.
shamefully-hoist=true
confirmModulesPurge=false
