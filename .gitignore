# upstream reference clone (StickmanRed/wick-editor) — has its own .git, do not vendor
reference/wick-editor/

# Rust
/target

# ruffle clone the golden-PNG oracle builds (exporter binary) — fetched, not source.
# Rebuild via scripts/oracle-setup.sh.
/oracle/

# plancheck hook debug dump — transient tool artifact
.plancheck-debug.json

# build artifacts (regenerate via cargo run)
/hello_square.swf
/test1.swf
/flipbook.swf
/tween.swf
/out.swf

# defn MCP server runtime state
/.defn/

# scratch
/tmp/
*.log

# session resume notes — transient
/RESUME.md

# editor / OS cruft
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/
